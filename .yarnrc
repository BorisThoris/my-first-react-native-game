# Transitive Electron tooling (e.g. @electron/rebuild) may declare a higher Node floor than the app needs.
# Ignore those dependency engine fields so `yarn` / `yarn add` work without per-command flags.
ignore-engines true
