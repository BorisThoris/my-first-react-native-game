import { StyleSheet } from 'react-native';

const livesStyles = StyleSheet.create({
    livesContainer: {
        flexDirection: 'row',
        justifyContent: 'center',
        marginBottom: 20
    },
    star: {
        color: '#FFD700',
        fontSize: 30,
        marginRight: 5
    }
});

export default livesStyles;
