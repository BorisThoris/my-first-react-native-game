import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    userContainer: {
        alignItems: 'center',
        padding: 10
    },
    userNameText: {
        fontSize: 18,
        fontWeight: 'bold'
    }
});

export default styles;
