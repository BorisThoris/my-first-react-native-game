import { StyleSheet } from 'react-native';

const cheaterTextStyles = StyleSheet.create({
    cheaterText: {
        color: 'red',
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 20,
        textAlign: 'center'
    }
});

export default cheaterTextStyles;
