# VIZ-004: Tier consistency (minimal / standard / full) bake vs runtime

## Problem

**minimal**, **standard**, and **full** [`OverlayDrawTier`](../../src/renderer/cardFace/overlayDrawTier.ts) values must stay aligned between **runtime** overlay drawing and **`yarn bake:procedural-set`** (`--tiers=`). CLI token mapping or quality-to-tier drift can cause silent mismatches.

## Proposed work

- Add a matrix test or script: same `pairKey` at each tier—compare dimensions and assert the tier changes output (not no-ops).
- Align naming: bake maps CLI tokens to tiers (see [`bake-procedural-illustration-set.ts`](../../scripts/bake-procedural-illustration-set.ts) `--tiers=` parsing) with [`OverlayDrawTier`](../../src/renderer/cardFace/overlayDrawTier.ts).

## Files

| Action | Path |
|--------|------|
| Read | [`scripts/bake-procedural-illustration-set.ts`](../../scripts/bake-procedural-illustration-set.ts) |
| Read | [`overlayDrawTier.ts`](../../src/renderer/cardFace/overlayDrawTier.ts), [`tileTextures.ts`](../../src/renderer/components/tileTextures.ts) |
| Add tests/scripts | TBD next to existing illustration tests or `e2e/` |

## Acceptance

- A missed tier mapping or quality-to-tier bug is caught by automation or a documented manual pass.

## Category

**bake/runtime**

## Priority

**P2**
