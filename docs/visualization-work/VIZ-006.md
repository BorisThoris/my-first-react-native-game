# VIZ-006: Downstream tooling — bake manifest → card-pipeline

## Status

**Done** — happy path documented in [README.md](./README.md) § Downstream: bake → card-pipeline.

## Problem

**`output/baked-procedural-illustrations/manifest.json`** can feed [`scripts/card-pipeline/`](../../scripts/card-pipeline/) (trim, AI briefs, comparisons), but the boundary is undefined—risk of duplicating docs or pulling ML inference into core gameplay.

## Proposed work

- Define optional **inputs and outputs** at the boundary (for example manifest path → trimmed PNG directory).
- Reference existing scripts by name once wired; avoid duplicating pipeline documentation in this folder.
- Keep ML training / cloud inference **out of core gameplay** unless explicitly productized elsewhere.

## Files

| Action | Path |
|--------|------|
| Read | [`scripts/card-pipeline/`](../../scripts/card-pipeline/), bake output layout in [`bake-procedural-illustration-set.ts`](../../scripts/bake-procedural-illustration-set.ts) |
| Add | Thin wrapper script or documented `package.json` script (TBD) |

## Acceptance

- One documented “happy path” (script or npm/yarn target) runs from bake output to a card-pipeline consumer.

## Category

**tooling**

## Priority

**P3**
