# UI-002 — Visual inventory Playwright reliability

**Priority:** P1  
**Status:** Done  
**Maps to:** [`PLAY-010`](../../new_design/TASKS/PLAYING_ENDPRODUCT/README.md) (evidence captures and inventory cadence).

## Evidence

`yarn capture:ui-audit` failures on: (1) `discoverMismatchPair` — transient `hidden === 2` during mismatch flip-back misclassified as stable downstate; (2) narrow landscape run-settings — toolbar button not scrolled into view; (3) long serial run — Classic Run CTA stability.

## Acceptance criteria

- `yarn capture:ui-audit` completes without the above systematic failures under Vite dev (serial 224-test run).

## Likely files

- [`e2e/visualScreenHelpers.ts`](../../../e2e/visualScreenHelpers.ts) — `settleAfterHiddenPairClick`, `startClassicRunFromModeSelect`.
- [`e2e/visualScenarioSteps.ts`](../../../e2e/visualScenarioSteps.ts) — run settings modal scenario.
- [`package.json`](../../../package.json) — `capture:ui-audit` sets `E2E_USE_SANDBOX_GAMEOVER=1` so `08-game-over` in the full device grid does not depend on the long-running live mismatch harness (see [`e2e/README.md`](../../../e2e/README.md)).
