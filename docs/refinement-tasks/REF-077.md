# REF-077: `TileBoardScene` disposal — textures, materials, listeners on unmount
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **071–080**).


## Problem

Three.js resources must be disposed when leaving Game screen to avoid GPU memory leaks on long sessions.

## Proposed work

- `useEffect` cleanup: traverse scene graph, `dispose()` geometries/materials, remove event listeners.
- Dev-only warning if texture count grows across navigation cycles.

## Files

| Action | Path |
|--------|------|
| Edit | `TileBoardScene.tsx` |

## Acceptance

- Heap/GPU memory flat after 10 enter/exit game cycles in profiler.

## Category

**performance** / **correctness**

## Priority

**P1**
