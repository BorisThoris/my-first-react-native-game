# REF-072: `OverlayModal` — focus return to last focused element on close
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **071–080**).


## Problem

Closing a modal should restore focus to the control that opened it for keyboard users.

## Proposed work

- Implement `returnFocus` using `document.activeElement` snapshot on open (or `aria-modal` pattern).
- RTL test opening from a button and asserting focus return.

## Files

| Action | Path |
|--------|------|
| Edit | `OverlayModal.tsx` |

## Acceptance

- Focus does not land on `<body>`; works for nested modals if supported.

## Category

**a11y**

## Priority

**P1**
