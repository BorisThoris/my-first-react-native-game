# REF-082: `GameScreen` polite live region — anti-spam throttle
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **081–090**).


## Problem

Frequent HUD updates can flood screen readers if every tick is announced.

## Proposed work

- Align `useHudPoliteLiveAnnouncement` cadence with gameplay events (batch within 300–500ms).
- Tests asserting announcement count caps per second in fixture scenarios.

## Files

| Action | Path |
|--------|------|
| Edit | `useHudPoliteLiveAnnouncement.ts`, `GameScreen.tsx` |

## Acceptance

- SR users get summaries, not per-frame chatter.

## Category

**a11y**

## Priority

**P1**
