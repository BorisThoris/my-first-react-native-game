# REF-092: TypeScript project references — `shared` vs `renderer` split
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **091–099**).


## Problem

Large TS projects benefit from project references for faster incremental builds and clearer boundaries.

## Proposed work

- Evaluate `composite` + `references` between `src/shared` and `src/renderer`; start with shared-only tsconfig.
- Measure `tsc -b` time vs single project.

## Files

| Action | Path |
|--------|------|
| Edit | `tsconfig.json`, nested configs |

## Acceptance

- No duplicate compilation of shared files; editor performance acceptable.

## Category

**tooling**

## Priority

**P3**
