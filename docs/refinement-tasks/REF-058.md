# REF-058: `breakpoints` — test environment and SSR safety
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **051–060**).


## Problem

`src/renderer/breakpoints.ts` may read `window` at import time; Vitest/SSR could diverge from browser.

## Proposed work

- Lazy-init listeners; default fallbacks when `window` undefined.
- Extend `breakpoints.test.ts` for mocked matchMedia sequences.

## Files

| Action | Path |
|--------|------|
| Edit | `breakpoints.ts`, `breakpoints.test.ts` |

## Acceptance

- No `window is not defined` in unit tests or static render path.

## Category

**correctness**

## Priority

**P2**
