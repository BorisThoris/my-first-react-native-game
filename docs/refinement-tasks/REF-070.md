# REF-070: `vitest.setup.ts` mock leakage across test files
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **061–070**).


## Problem

Global mocks in setup can leak state ordering dependencies between files.

## Proposed work

- Audit `beforeEach`/`afterEach` restoration for `matchMedia`, `Audio`, `ResizeObserver`.
- Enable `restoreMocks` / `clearMocks` if not already; document pattern for spies.

## Files

| Action | Path |
|--------|------|
| Edit | `vitest.setup.ts`, `vitest.config` |

## Acceptance

- Random test order (`vitest --sequence.shuffle`) passes locally.

## Category

**test** / **tooling**

## Priority

**P2**
