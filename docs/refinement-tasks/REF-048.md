# REF-048: `graphicsQuality` mapping to renderer + shader defines
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **041–050**).


## Problem

`src/shared/graphicsQuality.ts` selects tiered effects. Drift between quality enum and actual Three.js material flags causes “Medium” looking like “Low”.

## Proposed work

- Table in code or test: each `GraphicsQuality` → which features flip (shadows, particles, rim fire LOD).
- Assert `gameplayVisualConfig` / TileBoard scene reads the same mapping.

## Files

| Action | Path |
|--------|------|
| Edit | `graphicsQuality.ts`, `graphicsQuality.test.ts`, scene config |

## Acceptance

- One documented matrix; changing an enum updates all consumers.

## Category

**consistency**

## Priority

**P2**
