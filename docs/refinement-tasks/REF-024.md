# REF-024: RNG helpers: deterministic seeds and no accidental global mutation
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **021–030**).


## Problem

`src/shared/rng.ts` feeds gameplay and tests. Subtle changes (Math.random fallback, seed string normalization) break reproducibility and golden tests across platforms.

## Proposed work

- Unit tests: same seed string yields identical sequence across calls; forked RNGs are independent; document whether `rng` instances are safe to reuse after partial consumption.
- Add regression test if any function mutates a shared global (should not).

## Files

| Action | Path |
|--------|------|
| Add | `src/shared/rng.test.ts` |
| Read-only | `src/shared/rng.ts` |

## Acceptance

- `yarn test` passes; failures clearly indicate RNG behavior change.

## Category

**test**

## Priority

**P2**
