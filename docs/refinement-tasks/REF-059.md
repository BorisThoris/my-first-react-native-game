# REF-059: `desktop-client` vs web bootstrap — shared initialization checklist
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **051–060**).


## Problem

`src/renderer/desktop-client.ts` may duplicate startup steps from web entry (store hydration, feature flags, error boundaries).

## Proposed work

- Extract `initRendererShell({ platform })` used by both entries; document divergences (Steam vs browser).
- Single list of side effects run exactly once.

## Files

| Action | Path |
|--------|------|
| Edit | entrypoints under `src/renderer/` |

## Acceptance

- No double subscription to global events; easier onboarding for new platform ports.

## Category

**architecture**

## Priority

**P3**
