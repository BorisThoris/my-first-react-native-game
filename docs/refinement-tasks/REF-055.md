# REF-055: `gameplayVisualConfig` vs `contracts` — single source for timing
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **051–060**).


## Problem

`src/renderer/components/gameplayVisualConfig.ts` may duplicate durations that also exist in `src/shared/contracts.ts`, risking desync between rules and animation.

## Proposed work

- Import canonical timings from shared where applicable, or generate contracts subset for renderer-only effects with explicit naming.
- Add compile-time assertion that match-resolve window ≥ animation tail.

## Files

| Action | Path |
|--------|------|
| Edit | `gameplayVisualConfig.ts`, `contracts.ts` (minimal) |

## Acceptance

- One authoritative place per user-visible duration; grep-friendly.

## Category

**architecture**

## Priority

**P2**
