# REF-062: `tileBoardPick` parity with `game.ts` resolution for edge indices
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **061–070**).


## Problem

`src/renderer/components/tileBoardPick.ts` maps pointer → tile id. Off-by-one or rotated boards must match `game.ts` indexing.

## Proposed work

- Shared pure function tests: given board dimensions and transform, index matches `game` tile array order.
- Property tests for random sizes within supported range.

## Files

| Action | Path |
|--------|------|
| Edit | `tileBoardPick.test.ts`, `tileBoardPick.ts` |

## Acceptance

- Picking never selects a tile the rules engine considers out of bounds.

## Category

**test** / **correctness**

## Priority

**P1**
