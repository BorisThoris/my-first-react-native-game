# REF-060: `useCoarsePointer` — hybrid laptop / tablet false positives
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **051–060**).


## Problem

`src/renderer/hooks/useCoarsePointer.ts` infers input modality. Misclassification affects hit slop and HUD density.

## Proposed work

- Review `pointer: coarse` vs hover capability; add manual matrix (touchscreen laptop with mouse).
- Optional: prefer `any-pointer` / `pointer-events` level 2 hints.

## Files

| Action | Path |
|--------|------|
| Edit | `useCoarsePointer.ts`, tests with mocked media queries |

## Acceptance

- Documented behavior for common devices; no layout thrash on resize.

## Category

**UX**

## Priority

**P3**
