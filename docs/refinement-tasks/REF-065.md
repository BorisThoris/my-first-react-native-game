# REF-065: `save-data` nullish field migration matrix
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **061–070**).


## Problem

`src/shared/save-data.ts` evolves; older saves may omit new keys. `normalizeSaveData` must stay exhaustive.

## Proposed work

- Table-driven tests for each historical version fixture (minimal JSON blobs).
- Assert no `undefined` leaks into store after normalize.

## Files

| Action | Path |
|--------|------|
| Edit | `save-data.test.ts`, `save-data.ts` |

## Acceptance

- Each bump to save version includes new fixture row.

## Category

**test** / **reliability**

## Priority

**P1**
