# REF-044: `hudFixtures` parity with `GameplayHudBar` live props
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **041–050**).


## Problem

`src/renderer/dev/hudFixtures.ts` seeds HUD dev states. When `GameplayHudBar` gains props, fixtures can silently drift.

## Proposed work

- Type `hudFixtures` using the same props interface as production (pick/omit dev-only fields explicitly).
- Add a test that every fixture key maps to a valid render path.

## Files

| Action | Path |
|--------|------|
| Edit | `src/renderer/dev/hudFixtures.ts`, `hudFixtures.test.tsx` |

## Acceptance

- Fixture type errors surface at compile time when HUD API changes.

## Category

**test** / **dev**

## Priority

**P3**
