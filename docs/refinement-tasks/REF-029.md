# REF-029: `boardWebglPerfSample` stub vs real module in Vite/build
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **021–030**).


## Problem

Performance sampling may use a stub in some build modes (`boardWebglPerfSample.stub.ts`). Drift between stub and real exports breaks typecheck or silently disables sampling.

## Proposed work

- Ensure `vite.config` (or package exports) resolves one canonical implementation per target; align exported function signatures.
- Document in `dev/README` or adjacent comment when the stub is selected.

## Files

| Action | Path |
|--------|------|
| Edit | `vite.config.mts`, `src/renderer/dev/boardWebglPerfSample.ts`, `boardWebglPerfSample.stub.ts` |
| Tests | `src/renderer/dev/boardWebglPerfSample.test.ts` |

## Acceptance

- `yarn typecheck` and unit tests pass; no duplicate conflicting symbols.

## Category

**tooling**

## Priority

**P2**
