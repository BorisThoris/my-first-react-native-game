# REF-083: `useHudPoliteLiveAnnouncement` — batching API
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **081–090**).


## Problem

Multiple subsystems may call announce in one frame; duplicates should merge.

## Proposed work

- Expose `queuePoliteAnnouncement(text, { dedupeKey })`; flush on `requestAnimationFrame` boundary.
- Unit tests for dedupe and priority (errors > info).

## Files

| Action | Path |
|--------|------|
| Edit | `src/renderer/hooks/useHudPoliteLiveAnnouncement.ts`, tests |

## Acceptance

- Deterministic order; no duplicate strings with same key in one flush.

## Category

**a11y** / **architecture**

## Priority

**P2**
