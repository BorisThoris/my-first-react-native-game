# REF-046: `tileFieldTilt` vs `prefers-reduced-motion` and platform tilt
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **041–050**).


## Problem

`src/renderer/components/tileFieldTilt.ts` combines pointer/device tilt with motion preferences. Users who opt out of motion must not receive parallax.

## Proposed work

- Cross-check with `platformTilt` hooks and `gameplayVisualConfig`; single gate for “motion allowed”.
- Unit tests for `shouldApplyTilt` logic with mocked `matchMedia`.

## Files

| Action | Path |
|--------|------|
| Edit | `tileFieldTilt.ts`, `tileFieldTilt.test.ts`, platform tilt modules |

## Acceptance

- With reduced motion on, tilt contribution is zero; no conflicting toggles.

## Category

**a11y**

## Priority

**P1**
