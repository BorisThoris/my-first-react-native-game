# REF-030: Stabilize e2e flame / WebGL capture specs
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **021–030**).


## Problem

`e2e/capture-matched-flame.spec.ts` (and related) depend on timing and GPU state; flakes waste CI time and hide real regressions.

## Proposed work

- Prefer deterministic waits (aria, canvas readiness) over fixed `sleep` where possible.
- Align capture thresholds with `gameplayVisualConfig` / FX flags; document required env (headless WebGL).

## Files

| Action | Path |
|--------|------|
| Edit | `e2e/capture-matched-flame.spec.ts`, shared e2e helpers |
| Read-only | `src/renderer/components/gameplayVisualConfig.ts` |

## Acceptance

- Local + CI runs complete without intermittent screenshot failures under nominal load.

## Category

**test** / **e2e**

## Priority

**P1**
