# REF-064: `mutators.test.ts` — ritual when adding mutator metadata
> **Rollup:** Addressed in codebase per [COMPLETION.md](./COMPLETION.md) (evidence row **061–070**).


## Problem

`src/shared/mutators.ts` catalog grows; missing test coverage for new fields (stacking, exclusivity) allows inconsistent data.

## Proposed work

- Schema-style test: every mutator has required fields, valid `id` slug, non-empty display name.
- Optional: JSON schema snapshot for external tooling.

## Files

| Action | Path |
|--------|------|
| Edit | `mutators.test.ts` |

## Acceptance

- New mutator without metadata fields fails CI.

## Category

**test**

## Priority

**P2**
