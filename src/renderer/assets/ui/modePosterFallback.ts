/** Tiny sync import for hero cards before the async `modeArt` chunk loads (REF-074). */
import modePlaceholderUrl from './backgrounds/bg-mode-placeholder-v1.png';

export const MODE_POSTER_FALLBACK_URL = modePlaceholderUrl;
