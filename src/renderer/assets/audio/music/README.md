# Background music (`assets/audio/music`)

| File | Notes |
|------|--------|
| `chill-loop.wav` | Procedural loop from `yarn audio:placeholders` — styled to match UI art (**vault darkness + gold/ember warmth + crystal/cyan accents**): pad-forward ~72 BPM 4/4, **soft low-pass backbeat**, gentle kick, sparse filtered hats, **light rounded sub hits** + light arp. **16-bar** seamless loop (~53 s). Replace with an ACE-Step **text2music** export (rights-safe per `scripts/audio-pipeline/RIGHTS.md`) — see `jobs.music-chill.json`. |
