## Summary

<!-- What changed and why (1–3 sentences). -->

## Checklist

- [ ] `yarn typecheck` passes
- [ ] `yarn test` passes
- [ ] **Contracts / shared types** (`src/shared/contracts.ts` or exported save/run payloads touched): noted migrations or `normalizeSaveData` updates, and grepped `docs/` for stale type names (see REF-066).
