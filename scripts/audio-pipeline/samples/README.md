Place short WAV/MP3 references here when using `jobs.example.json` jobs **02** and **03**:

- `reference-for-text2music.wav` — style hint for `text2music` + `reference_audio`
- `source-for-cover.wav` — source track for `cover` + `src_audio`

Use material you have rights to use. Job **01** needs no samples.

If references are CC-BY, fill out **[ATTRIBUTION.template.txt](ATTRIBUTION.template.txt)** for shipped credits.
